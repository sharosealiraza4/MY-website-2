{
  "buildCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm run build:client",
  "outputDirectory": "dist/public",
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile"
}
