{
  "buildCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm exec vite build",
  "outputDirectory": "dist/public",
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile"
}
